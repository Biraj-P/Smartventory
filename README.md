# 2.0
-Adding postgreSQL and Redis for persistent database and cache management.

-Adding GenericJackson2JsonRedisSerializer for storing keys and values as JSON in Redis, so it can be human-readable.
